apiVersion: v1
kind: Service
metadata:
  name: proccessment-events-system-service
spec:
  selector:
    app: proccessment-events-system
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort